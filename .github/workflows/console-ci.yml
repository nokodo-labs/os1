name: console CI

on:
  workflow_call:
    inputs:
      run_tests:
        description: "run tests"
        type: boolean
        required: false
        default: true
      run_lint:
        description: "run linting"
        type: boolean
        required: false
        default: true
  workflow_dispatch:
    inputs:
      run_tests:
        description: "run tests"
        type: boolean
        required: false
        default: true
      run_lint:
        description: "run linting"
        type: boolean
        required: false
        default: true

jobs:
  quality:
    # the `file` matrix leg is the required leg; the carryover gate in main.yml
    # references this job name (`console CI / check quality on Node file`).
    name: check quality on Node ${{ matrix.node }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node:
          - file
          - "25"
    continue-on-error: ${{ matrix.node != 'file' }}
    defaults:
      run:
        shell: bash
        working-directory: console
    steps:
      - name: checkout
        uses: actions/checkout@v7

      - name: install pnpm
        uses: pnpm/action-setup@v6
        with:
          version: latest

      - name: set up Node from .nvmrc
        if: ${{ matrix.node == 'file' }}
        uses: actions/setup-node@v6
        with:
          node-version-file: console/.nvmrc
          check-latest: true
          cache: pnpm
          cache-dependency-path: console/pnpm-lock.yaml

      - name: set up Node ${{ matrix.node }}
        if: ${{ matrix.node != 'file' }}
        uses: actions/setup-node@v6
        with:
          node-version: "${{ matrix.node }}"
          check-latest: true
          cache: pnpm
          cache-dependency-path: console/pnpm-lock.yaml

      - name: install dependencies
        run: pnpm install --frozen-lockfile

      - name: check formatting
        run: pnpm exec prettier --check .

      - name: lint
        if: ${{ inputs.run_lint }}
        run: pnpm run lint

      - name: type check
        run: pnpm run check

      - name: run tests
        if: ${{ inputs.run_tests }}
        run: pnpm run test

      - name: build
        run: pnpm run build
